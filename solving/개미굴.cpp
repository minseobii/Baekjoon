#include <iostream>
using namespace std;
int N,ans;

int arr[15]
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> N;
    while(N--) {
        int K;
        cin >> K;
        for (int i=0;i<K;i++) {

        }
    }
    cout << ans << endl; 
    return 0;
}
